'use client'
import React, { ReactElement } from 'react';

const ContactPage = (): ReactElement => {
    return(
        <div>Do you want to build a shop for your business, need to launch a new API, or have a stubborn bug that is </div>
    );
};

export default ContactPage;